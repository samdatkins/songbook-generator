<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Add to Live Playlist</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.2/css/bulma.min.css"
    />
    <script
      defer
      src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"
    ></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <link
      rel="stylesheet"
      href="https://ajax.googleapis.com/ajax/libs/jqueryui/1.12.1/themes/smoothness/jquery-ui.css"
    />
    <script src="https://ajax.googleapis.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.js"></script>
    <link rel="stylesheet" href="/style/livePlaylist.css" />
    <script src="/scripts/tabSearchAutocomplete.js"></script>
  </head>
  <body>
    <section class="section">
      <div class="container">
        <p class="title"><%= powerHourTitle %></p>
        <% if (noodleMode === true) { %>
          <p><a href="index">Songbook Index</a></p>
          <BR>
          <% } %>
        <form method="POST" action="/live/<%= sessionKey %>/add">
          <div class="control">
            <button class="button is-link">Submit</button>
          </div>
          <br />

          <div class="field">
            <label class="label">Song Request</label>
            <div class="control">
              <input
                class="input"
                name="song"
                type="text"
                id="song"
                placeholder="Lynyrd Skynyrd - Freebird"
              />
            </div>
          </div>
        </form>
      </div>
    </section>
  </body>
</html>
